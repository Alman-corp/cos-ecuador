# Core packages needed at import time (lazy imports handle missing heavy deps)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.14.0
scikit-learn>=1.5.0
pmdarima>=2.0.4
statsmodels>=0.14.4
causal-learn>=0.1.3.9
# Optional heavy packages (installed with --extra flag or mounted volume):
# torch, dowhy, econml, pymc, arviz, prophet, darts
